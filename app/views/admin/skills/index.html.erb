<h1>Admin::Skills#index</h1>
<div>
  <% if params[:genre_id] %>
    <h2><%= @genre.name %>の商品一覧 (全<%= @all_skills_count %>件)</h2>
  <% else %>
    <h2>商品一覧 (全<%= @all_skills_count %>件)</h2>
  <% end %>
  <% if @skills.first %>
    <%= render 'admin/skills/index', skills: @skills %>
    <%= paginate @skills %>
  <% else %>
    <h3>対応する商品はありませんでした。</h3>
  <% end %>
</div>
